baseURL = "https://omar.ai/"
languageCode = "en-us"
title = "omar shabab"
theme = "noteworthy"
enableRobotsTXT = true
paginate = 10 # Set the number of posts to show before overflowing to the next page.
summaryLength = 10 # Configure how long the post summary should be on the homepage.

# Add your Google Analytics identifier: UA-XXXXXXXX-X
googleAnalytics = "UA-166869528-1" 

# Add your Disqus shortname here.
disqusShortname = "https-omar-ai"    

[taxonomies]
  tag = "tags"

[author]
	name = "omar shabab"

# Set to false to disallow raw HTML in markdown files
[markup.goldmark.renderer]
    unsafe = true

[params]
	math = true
	# Blog description at the top of the homepage. Supports markdown.
	description = "'writing for my own reference'"

    showReadMore = true

	# addThis

    # Set enableKofi to true to enable the Ko-fi support button. Add your Ko-fi ID to link to your account.
    # enableKofi = false
    # kofi = ""

	# Add links to your accounts. Remove the ones you don't want to include.
	# Main
	email = "mailto:omarshabab55@gmail.com"
    # facebook = "#"
    twitter = "https://twitter.com/omar161000/"
	# instagram = "#"
	# tumblr = "#"
	# reddit = "#"
	# pinterest = "#"
	# youtube = "#"
    # vimeo = "#"
	# weibo = "#"
	# vk = "#"
	linkedin = "https://www.linkedin.com/in/omar16100/"

	# Writing
    # medium = "#"
	# blogger = "#"
	# wordpress = "#"

	# Creative & Visual
    # dribbble = "#"
	# behance = "#"
	# deviantart = "#"
	# flickr = "#"

    # Audio & Music
    # soundcloud = "#"

	# Programming
    github = "https://github.com/omar16100"
	# stackoverflow = "#"
	# gitlab = "#"
	# codepen = "#"

  	# Academic
  	# googlescholar = "#"
  	# impactstory = "#"
  	# orcid = "#"  

# Menu links along the sidebar navigation.
[[menu.main]]
	identifier = "about"
	name = "About"
	url = "/about/"
	weight = 1 # Weight is an integer used to sort the menu items. The sorting goes from smallest to largest numbers. If weight is not defined for each menu entry, Hugo will sort the entries alphabetically.

[[menu.main]]
	identifier = "tags"
	name = "Tags"
	url = "/tags/"
	weight = 2

#[[menu.main]]
#	name = "Archives"
#	identifier = "archives"
#	url = "/archives/"
#	weight = 3

[[menu.main]]
	name = "Projects"
	identifier = "projects"
	url = "/projects/"
	weight = 3



# Privacy configurations: https://gohugo.io/about/hugo-and-gdpr/
[privacy]
  [privacy.disqus]
    disable = false
  [privacy.googleAnalytics]
    anonymizeIP = true
    disable = false
    respectDoNotTrack = false
    useSessionStorage = false
  [privacy.instagram]
    disable = false
    simple = false
  [privacy.twitter]
    disable = false
    enableDNT = false
    simple = false
  [privacy.vimeo]
    disable = false
    simple = false
  [privacy.youtube]
    disable = false
    privacyEnhanced = true


[[deployment.targets]]

name = "mydeployment"

# S3; see https://gocloud.dev/howto/blob/#s3
# For S3-compatible endpoints, see https://gocloud.dev/howto/blob/#s3-compatible
URL = "s3://omar.ai?region=ap-southeast-1"

# If you are using a CloudFront CDN, deploy will invalidate the cache as needed.
cloudFrontDistributionID = "E2JQ4U93EQ3PDI"

# [[deployment.matchers]] configure behavior for files that match the Pattern.
# Samples:

[[deployment.matchers]]
#  Cache static assets for 1 year.
pattern = "^.+\\.(js|css|svg|ttf)$"
cacheControl = "max-age=31536000, no-transform, public"
gzip = true

[[deployment.matchers]]
pattern = "^.+\\.(png|jpg)$"
cacheControl = "max-age=31536000, no-transform, public"
gzip = false

[[deployment.matchers]]
pattern = "^.+\\.(html|xml|json)$"
gzip = true
